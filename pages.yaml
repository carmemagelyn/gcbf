# Cloudflare Pages Deployment Configuration

## Build Configuration
build:
  command: npm run build
  publish: dist
  
## Environment
node_version: 18

## Redirects for Vue Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
